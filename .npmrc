# NPM Configuration for clean builds and Vercel compatibility
legacy-peer-deps=false
registry=https://registry.npmjs.org/

# Optional: Enable verbose logging for debugging
# loglevel=debug

# Optional: Set engine-strict to ensure Node version compatibility
# engine-strict=true
